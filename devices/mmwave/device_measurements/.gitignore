# 실제 측정 원본은 별도 보관하고 이 증거 레포에는 기본적으로 넣지 않는다.
raw/
*.jsonl
*.csv
*.wav
*.mp4
*.mov
!fixtures/*.jsonl

# 개인 식별 정보가 들어갈 수 있는 로컬 메모
private/
identifiers/

# Python runtime artifacts
__pycache__/
*.py[cod]
